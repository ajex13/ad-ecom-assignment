# PostgreSQL configuration
POSTGRES_HOST=localhost
POSTGRES_DB_NAME=ad_ecom_db
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# JWT / Auth
# A random long secret used to sign JWT access tokens
ACCESS_TOKEN_KEY=replace_with_a_strong_secret
# Token expiry (e.g. 15m, 1h, 7d)
ACCESS_TOKEN_EXPIRY=1h
# Optional: PEM public key (if using RSA) — leave empty for symmetric secret
PUBLIC_KEY=

# Application
PORT=3000
NODE_ENV=development
